<Page x:Class="ControllerManager.Views.ControllerPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      Background="#1E1E1E">

    <Grid Margin="40">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Border Grid.Row="0" Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,0,0,30">
            <TextBlock Text="{Binding ConnectionStatus}" FontSize="18" Foreground="White" HorizontalAlignment="Center"/>
        </Border>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="2*"/>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" VerticalAlignment="Top">
                <Border Background="#2D2D30" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="L Trigger" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                        <TextBlock Text="L Bumper" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Left Joystick +" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                        <TextBlock Text="Share Button" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                        <TextBlock Text="Left Button" FontSize="12" Foreground="#AAAAAA" Margin="0,2,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="DPAD" FontSize="14" Foreground="White" FontWeight="SemiBold" HorizontalAlignment="Center"/>
                    </StackPanel>
                </Border>
            </StackPanel>

            <Grid Grid.Column="1">
                <Border Background="#2D2D30" CornerRadius="8" Padding="40">
                    <Viewbox MaxHeight="300">
                        <Grid Width="400" Height="300">
                            <Ellipse Width="200" Height="200" Stroke="#666" StrokeThickness="3" 
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            <Ellipse Width="160" Height="160" Stroke="#666" StrokeThickness="2" 
                                     HorizontalAlignment="Center" VerticalAlignment="Center"/>
                            
                            <TextBlock Text="Change Controller Image&#x0a;depending on type" 
                                       FontSize="16" Foreground="#888" HorizontalAlignment="Center" 
                                       VerticalAlignment="Center" TextAlignment="Center"/>
                        </Grid>
                    </Viewbox>
                </Border>
            </Grid>

            <StackPanel Grid.Column="2" VerticalAlignment="Top">
                <Border Background="#2D2D30" Padding="20" CornerRadius="8">
                    <StackPanel>
                        <TextBlock Text="R Trigger" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                        <TextBlock Text="R Bumper" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="ABXY Buttons" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                        <TextBlock Text="Menu Button" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>

                <Border Background="#2D2D30" Padding="20" CornerRadius="8" Margin="0,20,0,0">
                    <StackPanel>
                        <TextBlock Text="Right Joystick" FontSize="14" Foreground="White" FontWeight="SemiBold"/>
                        <TextBlock Text="Right Button" FontSize="12" Foreground="#AAAAAA" Margin="0,5,0,0"/>
                    </StackPanel>
                </Border>
            </StackPanel>
        </Grid>
    </Grid>
</Page>
